import { SingleDatePipe } from './single-date.pipe';

describe('SingleDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SingleDatePipe();
    expect(pipe).toBeTruthy();
  });
});
